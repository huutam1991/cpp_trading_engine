#include <mongo_db/mongo_db.h>
#include <strategy/check_point.h>

void CheckPoint::save_to_db()
{
    // std::string collection_name = m_symbol + "_" + std::to_string(m_volumn) + "_" + std::to_string(m_volumn);
    // MongoDB::instance()
    //     .set_db_and_collection(STRATEGY_DB_NAME, collection_name)
}